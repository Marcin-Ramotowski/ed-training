---
name: frontend-design
description: Use this skill when the user asks to build web components, pages, or applications with high design quality. Triggers include phrases like "build a component", "create a page", "design a UI", "make an interface", "build a form/dashboard/landing page". Generates creative, production-grade code that avoids generic AI aesthetics.
---

Create a distinctive, production-grade frontend interface based on the user's requirements. Implement real working code with exceptional attention to aesthetic detail.

## Design Thinking

Before writing any code, commit to a bold aesthetic direction:

- **Purpose** — what problem does this interface solve? Who uses it?
- **Tone** — pick one extreme and own it: brutally minimal, maximalist chaos, retro-futuristic, organic/natural, luxury/refined, playful, editorial, brutalist, art deco, soft/pastel, industrial, etc.
- **Constraints** — framework, performance, accessibility requirements from the user.
- **Differentiation** — what makes this unforgettable? What is the one thing a user will remember?

Execute the chosen direction with precision. Bold maximalism and refined minimalism both work — intentionality is what matters, not intensity.

## Implementation Rules

- Write production-grade, functional code (HTML/CSS/JS, React, Vue, etc. — match what the user uses)
- **Typography**: choose distinctive, characterful fonts; avoid Inter, Roboto, Arial, system-ui
- **Color**: commit to a cohesive palette using CSS variables; dominant colors with sharp accents beat timid even distributions
- **Motion**: prefer CSS-only animations; use staggered reveals and scroll/hover states that surprise; one well-orchestrated load beats scattered micro-interactions
- **Layout**: use asymmetry, overlap, diagonal flow, grid-breaking elements, generous negative space or controlled density — never default grid layouts
- **Backgrounds**: create depth with gradient meshes, noise textures, geometric patterns, layered transparencies, dramatic shadows — never plain solid fills

## What to Avoid

- Purple gradients on white backgrounds, Space Grotesk, generic card grids, cookie-cutter hero sections
- Any layout or color scheme that looks like it could have been generated for any other project
- Designs that are "safe" — every output should be unmistakably context-specific

Match implementation complexity to the vision: maximalist designs need elaborate animations and effects; minimalist designs need precision spacing, micro-typography, and subtle detail. Elegance comes from executing the vision completely.
